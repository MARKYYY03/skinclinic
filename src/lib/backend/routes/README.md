This folder is intended for backend route-handler helpers, API endpoint logic, or route-specific services.

You can add files here for specific route behavior, for example:
- authRoutes.ts
- clientRoutes.ts
- transactionRoutes.ts

This folder is separate from the Supabase client and the backend auth helpers.
